import styled from 'styled-components';

const ButtonCell = styled('span')`
    display: flex;
    justify-content: end;
    border-bottom: none;
    svg {
        cursor: pointer;
    }
`;

export default ButtonCell;
